import React from 'react'
import { View } from 'react-native'
import { Text } from '@ui-kitten/components'
import { ISubway } from '../../interfaces'

const Subway = (subway: ISubway) => {
    const {  } = subway

	return (
		<View>
			<Text>{}</Text>
		</View>
	)
}

export default Subway
